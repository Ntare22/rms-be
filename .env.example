APP_ENV=development
PORT=8080

# libpq / Postgres URL used by GORM (see internal/database/postgres.go)
DATABASE_URL=postgres://rms:rms@localhost:5432/rms?sslmode=disable
APP_BASE_URL=http://localhost:5173
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
CORS_ALLOWED_METHODS=GET,POST,PUT,PATCH,DELETE,OPTIONS
CORS_ALLOWED_HEADERS=Origin,Content-Type,Accept,Authorization,X-Request-ID,X-Organization-ID,X-Internal-Key
CORS_EXPOSE_HEADERS=X-Request-ID
CORS_ALLOW_CREDENTIALS=true
CORS_MAX_AGE_SECONDS=43200

JWT_SECRET=change-me-access-secret
JWT_REFRESH_SECRET=change-me-refresh-secret
JWT_ACCESS_TTL_MINUTES=15
JWT_REFRESH_TTL_HOURS=168

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_FROM_NAME=

MAILJET_API_KEY=
MAILJET_API_SECRET=
MAILJET_FROM=
MAILJET_FROM_NAME=
MAILJET_FROM_EMAIL=
MAILJET_TEMPLATE_INVITE_ID=
MAILJET_TEMPLATE_BILLING_REMINDER_ID=

SMS_PROVIDER=
SMS_API_KEY=

# Optional: enables GET /internal/status (header X-Internal-Key)
INTERNAL_JOB_SECRET=

# Max POST /auth/login and /auth/register per client IP per minute (0 = disable).
RATE_LIMIT_AUTH_RPM=30

LOG_LEVEL=info
